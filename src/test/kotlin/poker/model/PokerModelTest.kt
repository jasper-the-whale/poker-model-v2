package poker.model

import org.junit.jupiter.api.Nested
import org.junit.jupiter.api.Test

internal class PokerModelTest {
    @Nested
    inner class Controller {
        @Nested
        inner class getHandOutcomes {
            @Nested
            inner class `when request is valid` {
                @Test
                fun `should return a valid api response`() {

                }
            }
        }
    }
}
